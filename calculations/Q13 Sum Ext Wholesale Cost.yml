unique_name: Q13 Sum Ext Wholesale Cost
object_type: metric_calc
label: Q13 Sum Ext Wholesale Cost
expression: >-
  SUM(
      FILTER(
          ([dimension_store_sales].[SS_SALES_PRICE].Members, [dimension_store_sales].[SS_NET_PROFIT].Members),
          [dimension_store_sales].[SS_SALES_PRICE].CurrentMember.MemberValue >= 100 AND
          [dimension_store_sales].[SS_SALES_PRICE].CurrentMember.MemberValue <= 150 AND
          [dimension_store_sales].[SS_NET_PROFIT].CurrentMember.MemberValue >= 100 AND
          [dimension_store_sales].[SS_NET_PROFIT].CurrentMember.MemberValue <= 300
      ),
      [Measures].[SS_EXT_WHOLESALE_COST]
  ) /* TODO: Converted from DAX To MDX using AI - please validate. Original DAX:
  CALCULATE( Sum(store_sales[SS_EXT_WHOLESALE_COST]),
  store_sales[SS_SALES_PRICE] >= 100 && store_sales[SS_SALES_PRICE] <= 150 && (
  store_sales[SS_NET_PROFIT] >= 100 && store_sales[SS_NET_PROFIT] <= 300) ) */
