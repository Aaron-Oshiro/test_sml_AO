unique_name: CalWrittenPremiumForRatio
object_type: metric_calc
label: CalWrittenPremiumForRatio
expression: >-
  0 /*CALCULATE( SUM( Premium[Premium_Amt] ), ALLEXCEPT( 'Amount Measure',
  'Amount Measure'[Total] ), 'Amount Measure'[Total] = "Written Premium" ,
  FILTER( ALL( 'Exposure Period'), CONTAINS( VALUES( 'Calendar Period'[Cal
  Month] ), 'Calendar Period'[Cal Month], 'Exposure Period'[Exposure Month] ) &&
  'Exposure Period'[Exposure Month] <= [MaxIBNRAsOfMt] ), ALLEXCEPT( CAT,
  CAT[Exposure YEAR] ), 'Policy Limits Attributes' ) TODO: Update with valid MDX
  expression*/
is_hidden: true
