unique_name: Q61 SS_ext_sales_price_sum
object_type: metric_calc
label: Q61 SS_ext_sales_price_sum
expression: >-
  SUM(
      FILTER(
          [Measures].[SS_EXT_SALES_PRICE],
          ([dimension_promotion].[p_channel_dmail].[Y], [promotion].[p_channel_email].[Y], [promotion].[p_channel_tv].[Y])
      )
  ) /* TODO: Converted from DAX To MDX using AI - please validate. Original DAX:
  CALCULATE( SUM(store_sales[SS_EXT_SALES_PRICE]), 'promotion'[p_channel_dmail]
  = "Y" || 'promotion'[p_channel_email] = "Y" || 'promotion'[p_channel_tv] = "Y"
  ) */
