unique_name: dimension_DIM_FLAG_FIDELIZACAO
object_type: dimension
label: DIM_FLAG_FIDELIZACAO
hierarchies:
  - unique_name: DIM_FLAG_FIDELIZACAO_Hierarchy
    label: DIM_FLAG_FIDELIZACAO Hierarchy
    description: DIM_FLAG_FIDELIZACAO Hierarchy
    levels:
      - unique_name: ID_FLAG_FIDELIZACAO
        secondary_attributes:
          - unique_name: Flag Fidelização
            label: Flag Fidelização
            dataset: dataset_DIM_FLAG_FIDELIZACAO
            key_columns:
              - Flag Fidelização
            name_column: Flag Fidelização
level_attributes:
  - unique_name: ID_FLAG_FIDELIZACAO
    label: ID_FLAG_FIDELIZACAO
    description: DIM_FLAG_FIDELIZACAO.ID_FLAG_FIDELIZACAO Level
    dataset: dataset_DIM_FLAG_FIDELIZACAO
    key_columns:
      - ID_FLAG_FIDELIZACAO
    name_column: ID_FLAG_FIDELIZACAO
type: standard
